# LaTeX 字体大小和透明度调整指南

## 📐 字体大小调整

### LaTeX标准字体大小命令（从小到大）

```latex
\tiny           % 最小
\scriptsize     % 很小
\footnotesize   % 脚注大小
\small          % 小
\normalsize     % 正常（默认）
\large          % 大
\Large          % 更大
\LARGE          % 很大
\huge           % 巨大
\Huge           % 最大
```

### 当前图表中的字体使用情况

| 元素 | 当前字体 | 位置示例 |
|------|----------|----------|
| **标题** | `\Large\bfseries` | Meta-PID Network Architecture |
| **副标题** | `\normalsize` | Hierarchical Meta-Learning... |
| **层标签** | `\bfseries` (normal) | Input, Encoder 1, Hidden |
| **维度标注** | `\small` | 10D, 256D, 128D |
| **左侧特征** | `\scriptsize` | Mass, DOF, Inertia |
| **层内操作** | `\scriptsize` | Linear, LayerNorm, ReLU |
| **Dropout标注** | `\tiny` | Dropout 0.1 |
| **输出标注** | `\small` | $\hat{K}_p \in [0,1]^7$ |
| **损失函数** | `\normalsize` | $\mathcal{L}_{meta} = ...$ |
| **信息框标题** | `\footnotesize\bfseries` | Network Parameters |
| **信息框内容** | `\footnotesize` | • Input: 10 |

---

## 🎨 透明度（Opacity）调整

### 透明度值范围

```latex
opacity=0.0    % 完全透明（不可见）
opacity=0.3    % 高度透明
opacity=0.5    % 半透明
opacity=0.7    % 轻微透明
opacity=0.9    % 几乎不透明
opacity=1.0    % 完全不透明（默认）
```

### 当前图表中的透明度使用

| 元素 | 当前透明度 | 位置 |
|------|------------|------|
| **立方体面** | `opacity=0.8-0.9` | 所有3D立方体 |
| **Dropout标签** | `opacity=0.7` | 灰色背景框 |
| **信息框阴影** | `drop shadow` | 底部三个信息框 |

---

## 🛠️ 具体修改方法

### 1. 修改层标签字体大小

**当前代码：**
```latex
\node[label=black] at (3.35, 5.0) {\textbf{Encoder 1}};
```

**修改为更大字体：**
```latex
\node[label=black] at (3.35, 5.0) {\large\textbf{Encoder 1}};  % 大
\node[label=black] at (3.35, 5.0) {\Large\textbf{Encoder 1}};  % 更大
```

**修改为更小字体：**
```latex
\node[label=black] at (3.35, 5.0) {\small\textbf{Encoder 1}};  % 小
```

---

### 2. 修改维度标注字体大小

**当前代码：**
```latex
\node[dimension, text=white] at (3.25, 4.3) {256D};
```

其中 `dimension` 样式定义为：
```latex
dimension/.style={font=\sffamily\small, text=gray}
```

**修改方法A：直接修改节点**
```latex
\node[text=white, font=\sffamily\footnotesize] at (3.25, 4.3) {256D};  % 更小
\node[text=white, font=\sffamily\normalsize] at (3.25, 4.3) {256D};    % 更大
```

**修改方法B：修改样式定义（推荐）**
在文件开头找到样式定义部分：
```latex
dimension/.style={font=\sffamily\small, text=gray},
```

改为：
```latex
dimension/.style={font=\sffamily\footnotesize, text=gray},  % 更小
dimension/.style={font=\sffamily\normalsize, text=gray},    % 更大
```

---

### 3. 修改左侧特征标注字体大小

**当前代码：**
```latex
\node[anchor=east, font=\sffamily\scriptsize, text width=2.5cm, align=right] 
     at (-0.3, 3.2) {Mass ($m$)};
```

**修改为更大字体：**
```latex
\node[anchor=east, font=\sffamily\small, text width=2.5cm, align=right] 
     at (-0.3, 3.2) {Mass ($m$)};  % 从scriptsize改为small
```

**修改为更小字体：**
```latex
\node[anchor=east, font=\sffamily\tiny, text width=2.5cm, align=right] 
     at (-0.3, 3.2) {Mass ($m$)};  % 改为tiny
```

---

### 4. 修改立方体透明度

**当前代码（在drawcube命令内部）：**
```latex
\fill[#6!100, opacity=0.9] (A) -- (B) -- (C) -- (D) -- cycle;  % 前面
\fill[#6!95, opacity=0.8] (B) -- (F) -- (G) -- (C) -- cycle;   % 右侧
\fill[#6!85, opacity=0.8] (E) -- (F) -- (B) -- (A) -- cycle;   % 底部
```

**修改方法：**
在文件第22-48行找到 `\newcommand{\drawcube}` 定义，修改opacity值：

```latex
% 更透明（轻盈感）
\fill[#6!100, opacity=0.7] (A) -- (B) -- (C) -- (D) -- cycle;
\fill[#6!95, opacity=0.6] (B) -- (F) -- (G) -- (C) -- cycle;
\fill[#6!85, opacity=0.6] (E) -- (F) -- (B) -- (A) -- cycle;

% 更不透明（厚重感）
\fill[#6!100, opacity=1.0] (A) -- (B) -- (C) -- (D) -- cycle;
\fill[#6!95, opacity=0.95] (B) -- (F) -- (G) -- (C) -- cycle;
\fill[#6!85, opacity=0.9] (E) -- (F) -- (B) -- (A) -- cycle;
```

---

### 5. 修改Dropout标签透明度

**当前代码：**
```latex
\node[fill=gray, opacity=0.7, rounded corners, text=white, font=\tiny] 
     at (3.25, -1.2) {Dropout 0.1};
```

**修改透明度：**
```latex
% 更透明
\node[fill=gray, opacity=0.5, rounded corners, text=white, font=\tiny] 
     at (3.25, -1.2) {Dropout 0.1};

% 更不透明
\node[fill=gray, opacity=0.9, rounded corners, text=white, font=\tiny] 
     at (3.25, -1.2) {Dropout 0.1};
```

**修改字体大小：**
```latex
% 更大字体
\node[fill=gray, opacity=0.7, rounded corners, text=white, font=\scriptsize] 
     at (3.25, -1.2) {Dropout 0.1};
```

---

### 6. 修改损失函数框字体和背景

**当前代码：**
```latex
\node[draw, thick, rounded corners, fill=yellow!20, text width=13cm, align=center,
      font=\sffamily\normalsize, drop shadow] at (8, -2.2)
{
    \textbf{Meta-Learning Loss Function:}\\[0.15cm]
    $\displaystyle \mathcal{L}_{meta} = \frac{1}{N}\sum_{v=1}^{N} ...
};
```

**修改字体大小：**
```latex
font=\sffamily\small         % 更小
font=\sffamily\large         % 更大
```

**修改背景颜色和透明度：**
```latex
fill=yellow!20               % 当前：20%黄色
fill=yellow!30               % 更深：30%黄色
fill=yellow!10               % 更浅：10%黄色
fill=yellow!20, opacity=0.8  % 添加整体透明度
```

---

### 7. 修改底部信息框

**当前代码：**
```latex
\node[draw, thick, rounded corners, fill=green!15, text width=3.2cm, align=left,
      font=\sffamily\footnotesize, drop shadow] at (2.5, -5.5)
{
    \textbf{Network Parameters:}\\[0.1cm]
    • Input: 10\\
    ...
};
```

**修改字体：**
```latex
font=\sffamily\scriptsize    % 更小
font=\sffamily\small         % 更大
```

**修改背景透明度：**
```latex
fill=green!15                % 当前：15%绿色
fill=green!20                % 更深
fill=green!10                % 更浅
```

**移除阴影：**
```latex
\node[draw, thick, rounded corners, fill=green!15, text width=3.2cm, align=left,
      font=\sffamily\footnotesize] at (2.5, -5.5)  % 去掉drop shadow
```

---

### 8. 修改输出标注（K符号）

**当前代码：**
```latex
\node[anchor=west, font=\sffamily\small] at (14.2, 4.2) 
    {$\hat{K}_p \in [0,1]^7$};
```

**修改字体大小：**
```latex
\node[anchor=west, font=\sffamily\footnotesize] at (14.2, 4.2) 
    {$\hat{K}_p \in [0,1]^7$};  % 更小

\node[anchor=west, font=\sffamily\normalsize] at (14.2, 4.2) 
    {$\hat{K}_p \in [0,1]^7$};  % 更大
```

---

## 📊 推荐配置方案

### 方案A：清晰型（适合演讲/海报）

```latex
% 层标签
\Large\bfseries              % 更大更醒目

% 维度标注
\normalsize                  % 正常大小

% 立方体透明度
opacity=0.85                 % 较不透明

% 信息框字体
\small                       % 更大更清晰
```

### 方案B：精致型（适合论文发表）- 当前配置

```latex
% 层标签
\bfseries                    % 正常加粗

% 维度标注
\small                       % 小号字体

% 立方体透明度
opacity=0.8-0.9              % 适中透明度

% 信息框字体
\footnotesize                % 脚注大小
```

### 方案C：紧凑型（适合空间有限）

```latex
% 层标签
\small\bfseries              % 小号加粗

% 维度标注
\footnotesize                % 更小

% 立方体透明度
opacity=0.75                 % 更透明

% 信息框字体
\scriptsize                  % 很小
```

---

## 🎯 快速修改指南

### 全局修改所有层标签字体

1. 找到所有 `\textbf{Input}`, `\textbf{Encoder 1}` 等
2. 在 `\textbf` 前添加字体大小命令
3. 例如：`{\Large\textbf{Input}}`

### 全局修改立方体透明度

1. 找到第22-48行的 `\newcommand{\drawcube}`
2. 修改所有 `opacity=` 后面的数值
3. 重新编译即可应用到所有立方体

### 全局修改信息框样式

1. 找到第212、225、238行的三个信息框
2. 统一修改 `font=` 和 `fill=` 参数
3. 保持三个框样式一致

---

## 🔧 调试技巧

### 1. 逐步调整

不要一次改太多，建议：
```
1. 先改一个元素
2. 编译查看效果
3. 满意后再改下一个
```

### 2. 对比测试

```latex
% 创建两个版本对比
\node[font=\small] at (x, y) {测试A};     % 版本1
% \node[font=\normalsize] at (x, y) {测试B};  % 版本2（注释掉）
```

### 3. 使用变量（高级）

在文件开头定义：
```latex
\newcommand{\layerlabelfont}{\Large\bfseries}
\newcommand{\dimfont}{\small}
```

使用时：
```latex
\node[font=\layerlabelfont] at (...) {Input};
```

这样只需修改一处定义即可全局更新！

---

## 📝 常见问题

### Q1: 修改后字体重叠怎么办？

**A:** 调整节点位置的y坐标，增大间距：
```latex
\node at (x, 4.5) {标题};    % 原来
\node at (x, 4.7) {标题};    % 改后（往上移）
```

### Q2: 透明度改了但看不出区别？

**A:** 可能是显示器问题或PDF渲染问题。建议：
- 透明度差值至少0.2才明显
- 在白色背景下查看效果

### Q3: 字体太大超出方块怎么办？

**A:** 三种解决方案：
1. 减小字体：`\small` → `\footnotesize`
2. 增大方块：修改 `\drawcube` 的宽度参数
3. 缩短文字：`Encoder 1` → `Enc 1`

---

## ✅ 修改检查清单

修改完成后请检查：

- [ ] 所有文字都在方块内，不超出边界
- [ ] 文字与边线有合理间距（不重叠）
- [ ] 同类元素字体大小一致
- [ ] 透明度适中，不影响可读性
- [ ] 编译无错误
- [ ] PDF显示正常
- [ ] 打印测试（如需要）

---

**文档版本：** 1.0  
**最后更新：** 2025-10-31  
**适用文件：** meta_pid_network_architecture.tex

