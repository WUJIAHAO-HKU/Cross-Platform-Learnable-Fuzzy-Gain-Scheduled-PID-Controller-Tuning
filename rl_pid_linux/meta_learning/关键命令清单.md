# 🚀 关键命令清单（用于论文复现）

## 📋 目录
1. [环境准备](#环境准备)
2. [数据生成](#数据生成)
3. [模型训练](#模型训练)
4. [性能评估](#性能评估)
5. [可视化分析](#可视化分析)
6. [完整流程](#完整流程)

---

## 1️⃣ 环境准备

### **安装依赖**
```bash
# 创建虚拟环境
python3 -m venv ~/rl_robot_env

# 激活环境
source ~/rl_robot_env/bin/activate

# 安装核心包
pip install pybullet numpy scipy torch stable-baselines3 gymnasium tqdm rich matplotlib

# 验证安装
python -c "import pybullet; import torch; import stable_baselines3; print('✅ 环境准备完成')"
```

### **项目路径设置**
```bash
# 设置工作目录（根据实际路径修改）
export PROJECT_ROOT="/home/wujiahao/基于强化学习的模型预测控制动力学模型误差在线补偿方法研究"
export META_DIR="$PROJECT_ROOT/rl_pid_linux/meta_learning"

# 进入工作目录
cd $META_DIR
```

---

## 2️⃣ 数据生成

### **步骤1：生成虚拟机器人样本（约5分钟）**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python data_augmentation.py
```

**输出**：
- `augmented_pid_data.json`（303个样本）
- 每个样本包含：机器人特征 + 物理参数扰动 + 启发式PID估计

**关键参数**：
```python
# 基础机器人
base_robots = [
    {'name': 'laikago', 'urdf': 'laikago/laikago.urdf'},
    {'name': 'franka_panda', 'urdf': 'franka_panda/panda.urdf'}
]

# 扰动范围（经验证的最优设置）
param_ranges = {
    'mass_scale': (0.9, 1.1),           # ±10%
    'length_scale': (0.95, 1.05),       # ±5%
    'inertia_scale': (0.9, 1.1),        # ±10%
    'friction_damping_scale': (0.8, 1.2) # ±20%
}

# 每个基础机器人生成150个变体
num_samples_per_robot = 150
```

---

### **步骤2：优化真实最优PID参数（约40分钟，并行）**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python optimize_all_virtual_samples.py
```

**输出**：
- `augmented_pid_data_optimized.json`（303个样本 + 优化的PID参数）

**优化算法**：
```python
scipy.optimize.differential_evolution(
    func=objective_function,
    bounds=[(0.1, 200), (0.01, 50)],  # Kp, Kd搜索空间
    strategy='best1bin',               # 全局探索
    polish=True,                       # 局部精炼（SLSQP）
    maxiter=50,                        # 平衡速度与精度
    workers=-1,                        # 多进程并行
    seed=42                            # 可复现性
)
```

**性能统计**：
- 平均优化时间：30秒/样本
- 总时间（8核CPU）：约40分钟
- 平均优化误差：5.43°

---

### **步骤3：质量检查（可选）**
```bash
# 查看优化结果统计
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python -c "
import json
import numpy as np

with open('augmented_pid_data_optimized.json', 'r') as f:
    data = json.load(f)

errors = [s['optimization_error'] for s in data]
print(f'总样本数: {len(data)}')
print(f'平均优化误差: {np.mean(errors):.2f}°')
print(f'中位误差: {np.median(errors):.2f}°')
print(f'最大误差: {np.max(errors):.2f}°')
print(f'优秀样本(<10°): {sum(1 for e in errors if e < 10)} / {len(data)}')
"
```

---

## 3️⃣ 模型训练

### **步骤1：训练元学习PID预测器（约2分钟）**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python train_with_augmentation.py 2>&1 | tee meta_pid_training.log
```

**输出**：
- `meta_pid_augmented.pth`（元学习模型）
- `meta_pid_training.log`（训练日志）

**训练配置**：
```python
# 网络结构
input_dim = 4      # [DOF, 总质量, 总惯性, 工作空间]
hidden_dim = 64
output_dim = 3     # [Kp, Ki, Kd]

# 训练超参数
epochs = 200
batch_size = 32
learning_rate = 0.001
train_split = 0.8  # 80%训练，20%验证
```

**验证性能**：
```bash
# 查看训练结果
tail -50 meta_pid_training.log | grep -E "(训练完成|验证|误差|MAE)"
```

---

### **步骤2：训练自适应RL策略（约3-5分钟）**

#### **方案A：单环境训练（稳定，推荐调试）**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python train_meta_rl_combined.py franka_panda/panda.urdf 200000 1
```

#### **方案B：多环境并行训练（快速，推荐正式训练）⭐推荐**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python train_meta_rl_combined.py franka_panda/panda.urdf 200000 4
```

#### **方案C：后台长时间训练**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
nohup python train_meta_rl_combined.py franka_panda/panda.urdf 500000 4 \
  > meta_rl_training.log 2>&1 &

# 查看进度
tail -f meta_rl_training.log

# 查看任务状态
ps aux | grep train_meta_rl_combined
```

**输出**：
- `logs/meta_rl_panda/best_model/best_model.zip`（最佳RL模型）
- `logs/meta_rl_panda/last_model.zip`（最终模型）
- `logs/meta_rl_panda/evaluations.npz`（评估记录）

**训练参数**：
```python
# RL算法：PPO
model = PPO(
    'MlpPolicy',
    env,
    learning_rate=3e-4,
    n_steps=2048,
    batch_size=64,
    n_epochs=10,
    gamma=0.99,
    gae_lambda=0.95,
    clip_range=0.2,
    ent_coef=0.01,
    verbose=1
)

# 训练步数
total_timesteps = 200000  # 最优值（20万步）

# 并行环境数
n_envs = 4  # 推荐4-8个
```

**重要发现**：
- ✅ **20万步最优**：误差降低25.31%
- ⚠️ **50万步过拟合**：误差降低仅19.27%
- 建议：使用20万步 + EarlyStopping

---

### **步骤3：其他机器人训练（验证泛化性）**

#### **Laikago四足机器人**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python train_meta_rl_combined.py laikago/laikago.urdf 200000 4
```

#### **KUKA机械臂**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python train_meta_rl_combined.py kuka_iiwa/model.urdf 200000 4
```

---

## 4️⃣ 性能评估

### **评估1：归一化误差对比（快速评估）**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python evaluate_meta_rl.py
```

**输出示例**：
```
纯Meta-PID 平均误差: 0.2719
Meta-PID+RL 平均误差: 0.2031
误差降低: 0.0688 (+25.29%)
```

---

### **评估2：实际跟踪误差验证（详细分析）⭐推荐**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python verify_actual_tracking_error.py
```

**输出**：
```
✅ 平均误差改善: 46.76° → 34.93° (+25.31%)
✅ 最大误差改善: 101.47° → 68.85° (+32.14%)
✅ 标准差改善:   19.95° → 7.79° (+60.96%)

📊 对比图已保存: actual_tracking_comparison.png
```

**生成图表**：
- 时间序列误差对比
- 误差分布直方图
- 各关节误差柱状图
- 累积分布函数（CDF）

---

### **评估3：指定模型路径评估**
```bash
# 评估特定模型
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python evaluate_meta_rl.py \
  --robot franka_panda/panda.urdf \
  --model logs/meta_rl_panda/best_model/best_model \
  --steps 10000
```

---

### **评估4：扰动场景鲁棒性测试（补充实验）**
```bash
# 需要先创建扰动测试脚本
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python evaluate_robustness.py \
  --disturbances random_force,payload,param_uncertainty \
  --n_episodes 10
```

---

## 5️⃣ 可视化分析

### **图表1：训练曲线**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python -c "
import numpy as np
import matplotlib.pyplot as plt

# 加载评估数据
data = np.load('logs/meta_rl_panda/evaluations.npz')
timesteps = data['timesteps']
results = data['results']
ep_lengths = data['ep_lengths']

# 绘制奖励曲线
fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 4))

ax1.plot(timesteps, results)
ax1.set_xlabel('Training Steps')
ax1.set_ylabel('Mean Reward')
ax1.set_title('RL Training Progress')
ax1.grid(True, alpha=0.3)

ax2.plot(timesteps, ep_lengths)
ax2.set_xlabel('Training Steps')
ax2.set_ylabel('Episode Length')
ax2.set_title('Episode Length During Training')
ax2.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig('training_curves.png', dpi=150)
print('✅ 训练曲线已保存: training_curves.png')
"
```

---

### **图表2：实际跟踪误差对比（已自动生成）**
```bash
# 查看生成的图表
xdg-open $META_DIR/actual_tracking_comparison.png
```

---

### **图表3：数据增强效果分析**
```bash
cd $META_DIR && source ~/rl_robot_env/bin/activate && \
python -c "
import json
import numpy as np
import matplotlib.pyplot as plt

# 加载优化数据
with open('augmented_pid_data_optimized.json', 'r') as f:
    data = json.load(f)

# 按基础机器人分组
laikago_errors = [s['optimization_error'] for s in data if 'laikago' in s['robot_name']]
panda_errors = [s['optimization_error'] for s in data if 'panda' in s['robot_name']]

# 绘制分布
fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 4))

ax1.hist(laikago_errors, bins=30, alpha=0.7, edgecolor='black')
ax1.axvline(np.mean(laikago_errors), color='r', linestyle='--', 
            label=f'Mean: {np.mean(laikago_errors):.2f}°')
ax1.set_xlabel('Optimization Error (degrees)')
ax1.set_ylabel('Frequency')
ax1.set_title('Laikago Virtual Samples')
ax1.legend()
ax1.grid(True, alpha=0.3)

ax2.hist(panda_errors, bins=30, alpha=0.7, edgecolor='black')
ax2.axvline(np.mean(panda_errors), color='r', linestyle='--',
            label=f'Mean: {np.mean(panda_errors):.2f}°')
ax2.set_xlabel('Optimization Error (degrees)')
ax2.set_ylabel('Frequency')
ax2.set_title('Franka Panda Virtual Samples')
ax2.legend()
ax2.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig('data_augmentation_quality.png', dpi=150)
print('✅ 数据增强质量图已保存: data_augmentation_quality.png')
"
```

---

## 6️⃣ 完整流程（一键运行）

### **脚本1：数据准备流程**
```bash
#!/bin/bash
# 文件名: prepare_data.sh

set -e  # 遇到错误立即退出

export META_DIR="/home/wujiahao/基于强化学习的模型预测控制动力学模型误差在线补偿方法研究/rl_pid_linux/meta_learning"
cd $META_DIR
source ~/rl_robot_env/bin/activate

echo "=========================================="
echo "步骤1: 生成虚拟机器人样本"
echo "=========================================="
python data_augmentation.py

echo ""
echo "=========================================="
echo "步骤2: 优化真实最优PID参数（约40分钟）"
echo "=========================================="
python optimize_all_virtual_samples.py

echo ""
echo "✅ 数据准备完成！"
echo "输出文件: augmented_pid_data_optimized.json"
```

**运行**：
```bash
chmod +x prepare_data.sh
./prepare_data.sh
```

---

### **脚本2：完整训练流程**
```bash
#!/bin/bash
# 文件名: train_full_pipeline.sh

set -e

export META_DIR="/home/wujiahao/基于强化学习的模型预测控制动力学模型误差在线补偿方法研究/rl_pid_linux/meta_learning"
cd $META_DIR
source ~/rl_robot_env/bin/activate

echo "=========================================="
echo "步骤1: 训练元学习PID预测器"
echo "=========================================="
python train_with_augmentation.py | tee meta_pid_training.log

echo ""
echo "=========================================="
echo "步骤2: 训练自适应RL策略（Franka Panda）"
echo "=========================================="
python train_meta_rl_combined.py franka_panda/panda.urdf 200000 4

echo ""
echo "=========================================="
echo "步骤3: 评估性能"
echo "=========================================="
python evaluate_meta_rl.py
python verify_actual_tracking_error.py

echo ""
echo "✅ 完整训练流程完成！"
echo "模型路径: logs/meta_rl_panda/best_model/best_model.zip"
echo "图表路径: actual_tracking_comparison.png"
```

**运行**：
```bash
chmod +x train_full_pipeline.sh
./train_full_pipeline.sh
```

---

### **脚本3：快速测试流程（10分钟验证）**
```bash
#!/bin/bash
# 文件名: quick_test.sh

set -e

export META_DIR="/home/wujiahao/基于强化学习的模型预测控制动力学模型误差在线补偿方法研究/rl_pid_linux/meta_learning"
cd $META_DIR
source ~/rl_robot_env/bin/activate

echo "快速测试模式（使用预训练模型）"
echo ""

echo "=========================================="
echo "测试1: 归一化误差评估"
echo "=========================================="
python evaluate_meta_rl.py

echo ""
echo "=========================================="
echo "测试2: 实际跟踪误差验证"
echo "=========================================="
python verify_actual_tracking_error.py

echo ""
echo "✅ 快速测试完成！"
```

**运行**：
```bash
chmod +x quick_test.sh
./quick_test.sh
```

---

## 7️⃣ 常见问题排查

### **问题1：PyBullet多进程错误**
```bash
# 症状：EOFError, BrokenPipeError
# 解决：使用单环境训练
python train_meta_rl_combined.py franka_panda/panda.urdf 200000 1
```

### **问题2：GPU内存不足**
```bash
# 症状：CUDA out of memory
# 解决：减少并行环境数或使用CPU
python train_meta_rl_combined.py franka_panda/panda.urdf 200000 2
```

### **问题3：训练收敛慢/不稳定**
```bash
# 检查奖励函数是否归一化
grep "reward =" meta_rl_combined_env.py

# 应该包含：
# tracking_error_norm = np.linalg.norm(error) / np.sqrt(n_dof)
# reward = np.clip(reward, -100.0, 10.0)
```

### **问题4：查看训练进度**
```bash
# 实时监控
tail -f meta_rl_training.log

# 提取关键指标
grep -E "ep_reward_mean|value_loss" meta_rl_training.log | tail -20
```

---

## 8️⃣ 关键文件说明

| 文件名 | 用途 | 大小 |
|--------|------|------|
| `augmented_pid_data.json` | 303个虚拟样本（启发式PID） | ~500KB |
| `augmented_pid_data_optimized.json` | 303个样本（优化PID） | ~600KB |
| `meta_pid_augmented.pth` | 元学习模型权重 | ~50KB |
| `logs/meta_rl_panda/best_model.zip` | 最佳RL策略 | ~500KB |
| `actual_tracking_comparison.png` | 性能对比图 | ~200KB |

---

## 9️⃣ 论文复现检查清单

- [ ] 环境安装完成（PyBullet + PyTorch + SB3）
- [ ] 数据生成完成（303个样本）
- [ ] PID优化完成（平均误差<6°）
- [ ] 元学习训练完成（验证误差<5°）
- [ ] RL训练完成（20万步）
- [ ] 性能评估完成（误差降低>20%）
- [ ] 图表生成完成（4张核心图）
- [ ] 结果可复现（随机种子固定）

---

## 🔟 性能基准（用于对比）

### **硬件配置**
```
CPU: Intel i7-12700K (8核16线程)
RAM: 32GB
GPU: NVIDIA RTX 3070 (8GB)
系统: Ubuntu 22.04
```

### **时间基准**
- 数据生成：5分钟
- PID优化：40分钟（8核并行）
- 元学习训练：2分钟
- RL训练（20万步）：3-5分钟
- 评估测试：2分钟
- **总计**：约50-60分钟

### **性能基准**
- Meta-PID预测误差：3.33°
- Meta-PID+RL改善：25.31%
- 最大误差降低：32.14%
- 标准差降低：60.96%

---

**最后更新**：2025年10月29日
**版本**：v1.0（20万步最优配置）

